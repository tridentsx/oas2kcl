schema Multi:

    # This schema has alternative validation rules (oneOf/anyOf)
    # KCL doesn't directly support these OpenAPI constructs
    # oneOf validation requires exactly one of the following schemas to be valid:
    # Option 1: 
    # Option 2: 
